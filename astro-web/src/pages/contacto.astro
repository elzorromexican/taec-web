---
import BaseLayout from '../layouts/BaseLayout.astro';
import { contactData } from '../data/contact';
import { emailjsConfig } from '../data/emailjs';

const { publicKey, serviceId, templates } = emailjsConfig;
const templateId = templates.contact;
const contactEmail = contactData.email;
---

<BaseLayout
  title="Contacto – TAEC | Habla con un especialista en e-learning"
  description="Habla con un especialista TAEC. Diagnóstico gratuito para tu estrategia de capacitación corporativa. Respondemos en menos de 24 horas."
  section=""
  page="contacto.html"
>
  <style is:global>

  /* ── PAGE HERO ── */
    .page-hero {
      background: linear-gradient(135deg, var(--navy) 0%, #00355a 100%);
      padding: 56px 5% 60px;
      text-align: center;
    }
    .page-hero-inner { max-width: 640px; margin: 0 auto; }
    .page-hero-tag {
      display: inline-block;
      background: rgba(168,219,217,.15); color: var(--teal);
      font-size: 11px; font-weight: 700; letter-spacing: 1.5px;
      text-transform: uppercase; padding: 4px 12px; border-radius: 4px;
      margin-bottom: 16px;
    }
    .page-hero h1 {
      font-family: var(--font-display);
      font-size: 38px; color: var(--white); line-height: 1.15;
      margin-bottom: 14px;
    }
    .page-hero p {
      color: #94A3B8; font-size: 16px; line-height: 1.75;
    }

    /* ── CONTACT LAYOUT ── */
    .contact-section {
      padding: 64px 5%;
      background: var(--light);
    }
    .contact-inner {
      max-width: 1080px; margin: 0 auto;
      display: grid; grid-template-columns: 1fr 1.6fr; gap: 48px;
      align-items: start;
    }

    /* ── INFO SIDEBAR ── */
    .contact-info { display: flex; flex-direction: column; gap: 28px; }

    .info-block h3 {
      font-size: 13px; font-weight: 700; text-transform: uppercase;
      letter-spacing: 1px; color: var(--muted); margin-bottom: 12px;
    }

    .info-item {
      display: flex; align-items: flex-start; gap: 12px;
      padding: 16px; background: var(--white);
      border: 1px solid var(--border); border-radius: 10px;
      margin-bottom: 10px;
    }
    .info-item:last-child { margin-bottom: 0; }
    .info-icon {
      width: 38px; height: 38px; border-radius: 8px;
      background: var(--light); display: flex; align-items: center;
      justify-content: center; font-size: 16px; flex-shrink: 0;
    }
    .info-content { flex: 1; }
    .info-content strong {
      display: block; font-size: 13px; font-weight: 600;
      color: var(--text); margin-bottom: 2px;
    }
    .info-content a, .info-content span {
      font-size: 13px; color: var(--muted);
    }
    .info-content a:hover { color: var(--blue); }

    .response-badge {
      display: flex; align-items: center; gap: 10px;
      background: #EFF8F7; border: 1px solid #C6E8E7;
      border-radius: 10px; padding: 14px 16px;
    }
    .response-badge .dot {
      width: 8px; height: 8px; border-radius: 50%;
      background: #2ECC71; flex-shrink: 0;
      box-shadow: 0 0 0 3px rgba(46,204,113,.2);
    }
    .response-badge p {
      font-size: 13px; color: var(--dark); line-height: 1.4;
    }
    .response-badge strong { color: var(--navy); }

    /* ── FORM CARD ── */
    .form-card {
      background: var(--white);
      border: 1px solid var(--border);
      border-radius: 16px; padding: 36px;
    }
    .form-card h2 {
      font-family: var(--font-display);
      font-size: 24px; color: var(--navy);
      margin-bottom: 6px;
    }
    .form-card .form-subtitle {
      font-size: 14px; color: var(--muted); margin-bottom: 28px;
    }

    .form-row { display: grid; grid-template-columns: 1fr 1fr; gap: 16px; }

    .field { display: flex; flex-direction: column; gap: 6px; margin-bottom: 18px; }
    .field label {
      font-size: 13px; font-weight: 600; color: var(--dark);
    }
    .field label .req { color: var(--coral); margin-left: 2px; }
    .field input, .field select, .field textarea {
      font-family: var(--font-body); font-size: 14px;
      color: var(--text); background: var(--white);
      border: 1.5px solid var(--border); border-radius: 8px;
      padding: 10px 14px; transition: border-color .15s, box-shadow .15s;
      outline: none;
    }
    .field input:focus, .field select:focus, .field textarea:focus {
      border-color: var(--blue);
      box-shadow: 0 0 0 3px rgba(49,121,194,.1);
    }
    .field input[aria-invalid="true"], .field select[aria-invalid="true"], .field textarea[aria-invalid="true"] {
      border-color: #EF4444;
    }
    .field-error {
      font-size: 12px; color: #EF4444; margin-top: 2px;
      display: none;
    }
    .field-error.visible { display: block; }
    .field textarea { resize: vertical; min-height: 110px; }
    .field select { cursor: pointer; }

    .form-footer {
      display: flex; align-items: center; justify-content: space-between;
      gap: 16px; flex-wrap: wrap; margin-top: 8px;
    }
    .form-note { font-size: 12px; color: var(--muted); }

    .btn-submit {
      display: inline-flex; align-items: center; gap: 8px;
      background: var(--orange); color: var(--white);
      font-family: var(--font-body); font-size: 14px; font-weight: 700;
      padding: 12px 28px; border-radius: 8px; border: none;
      cursor: pointer; transition: background .15s;
    }
    .btn-submit:hover:not(:disabled) { background: #d97b0e; }
    .btn-submit:disabled { background: #94A3B8; cursor: not-allowed; }

    /* ── SUCCESS / ERROR STATES ── */
    .form-status {
      display: none; padding: 14px 16px;
      border-radius: 8px; font-size: 14px; font-weight: 500;
      margin-top: 16px;
    }
    .form-status.success {
      display: block;
      background: #EFF8F7; border: 1px solid #C6E8E7; color: #1a7a6e;
    }
    .form-status.error {
      display: block;
      background: #FEF2F2; border: 1px solid #FECACA; color: #b91c1c;
    }

    /* ── RESPONSIVE ── */
    @media (max-width: 1024px) {
      .contact-inner { grid-template-columns: 1fr; }
    }
    @media (max-width: 768px) {
      .page-hero h1 { font-size: 28px; }
      .form-card { padding: 24px; }
      .form-row { grid-template-columns: 1fr; }
      .form-footer { flex-direction: column; align-items: stretch; }
      .btn-submit { justify-content: center; }
    }

  </style>

  <!-- EmailJS CDN — loaded before the form script -->
  <script is:inline src="https://cdn.jsdelivr.net/npm/@emailjs/browser@4/dist/email.min.js"></script>

<main id="main-content">

<!-- ══ HERO ══ -->
<section class="page-hero">
  <div class="page-hero-inner">
    <span class="page-hero-tag">Contacto</span>
    <h1>Hablemos de tu proyecto</h1>
    <p>Cuéntanos qué necesitas — en menos de 24 horas un especialista te responde con ideas concretas, sin compromiso.</p>
  </div>
</section>

<!-- ══ FORMULARIO ══ -->
<section class="contact-section">
  <div class="contact-inner">

    <!-- Info lateral -->
    <div class="contact-info">

      <div class="info-block">
        <h3>Escríbenos</h3>
        <div class="info-item">
          <div class="info-icon">✉</div>
          <div class="info-content">
            <strong>Correo electrónico</strong>
            <a href={`mailto:${contactData.email}`}>{contactData.email}</a>
          </div>
        </div>
        <div class="info-item">
          <div class="info-icon">📞</div>
          <div class="info-content">
            <strong>Teléfono CDMX</strong>
            <a href={`tel:${contactData.phone.replace(/ /g,'')}`}>{contactData.phone}</a>
          </div>
        </div>
        <div class="info-item">
          <div class="info-icon">💬</div>
          <div class="info-content">
            <strong>WhatsApp</strong>
            <a href={contactData.whatsapp.url} target="_blank" rel="noopener noreferrer">+52 1 55 2775 8279</a>
          </div>
        </div>
        <div class="info-item">
          <div class="info-icon">in</div>
          <div class="info-content">
            <strong>LinkedIn</strong>
            <a href={contactData.socials.linkedin} target="_blank" rel="noopener noreferrer">linkedin.com/company/taec</a>
          </div>
        </div>
      </div>

      <div class="response-badge">
        <div class="dot"></div>
        <p><strong>Respondemos en menos de 24 h</strong><br>Lunes a viernes, 9:00 – 18:00 (CDMX)</p>
      </div>

    </div><!-- /contact-info -->

    <!-- Formulario -->
    <div class="form-card">
      <h2>Cuéntanos qué necesitas</h2>
      <p class="form-subtitle">Diagnóstico gratuito · Sin compromiso · Respuesta en menos de 24 h</p>

      <form id="contactForm" novalidate>

        <div class="form-row">
          <div class="field">
            <label for="nombre">Nombre <span class="req" aria-hidden="true">*</span></label>
            <input type="text" id="nombre" name="nombre" placeholder="Tu nombre"
              required aria-required="true" autocomplete="given-name" />
            <span class="field-error" id="err-nombre">Por favor ingresa tu nombre.</span>
          </div>
          <div class="field">
            <label for="empresa">Empresa <span class="req" aria-hidden="true">*</span></label>
            <input type="text" id="empresa" name="empresa" placeholder="Nombre de tu organización"
              required aria-required="true" autocomplete="organization" />
            <span class="field-error" id="err-empresa">Por favor ingresa tu empresa.</span>
          </div>
        </div>

        <div class="form-row">
          <div class="field">
            <label for="email">Correo electrónico <span class="req" aria-hidden="true">*</span></label>
            <input type="email" id="email" name="email" placeholder="tu@empresa.com"
              required aria-required="true" autocomplete="email" />
            <span class="field-error" id="err-email">Por favor ingresa un correo válido.</span>
          </div>
          <div class="field">
            <label for="telefono">Teléfono / WhatsApp</label>
            <input type="tel" id="telefono" name="telefono" placeholder="+52 55 …" autocomplete="tel" />
          </div>
        </div>

        <div class="field">
          <label for="tema">¿En qué podemos ayudarte? <span class="req" aria-hidden="true">*</span></label>
          <select id="tema" name="tema" required aria-required="true">
            <option value="" disabled selected>Selecciona una opción</option>
            <option value="Articulate 360">Articulate 360 — licencias / capacitación</option>
            <option value="Moodle / LMS">Moodle / LMS — implementación y soporte</option>
            <option value="Vyond">Vyond — licencias y producción de videos</option>
            <option value="Totara LMS">Totara LMS — plataforma enterprise</option>
            <option value="Cursos a medida">Cursos a medida (diseño instruccional)</option>
            <option value="Consultoría L&D">Consultoría L&D / estrategia de capacitación</option>
            <option value="Otro">Otro</option>
          </select>
          <span class="field-error" id="err-tema">Por favor selecciona un tema.</span>
        </div>

        <div class="field">
          <label for="mensaje">Cuéntanos más <span class="req" aria-hidden="true">*</span></label>
          <textarea id="mensaje" name="mensaje"
            placeholder="¿Cuántas personas? ¿Qué herramientas usan hoy? ¿Cuál es el reto principal?…"
            required aria-required="true"></textarea>
          <span class="field-error" id="err-mensaje">Por favor escribe tu mensaje.</span>
        </div>

        <div class="form-footer">
          <span class="form-note">🔒 Tu información es confidencial. No hacemos spam.</span>
          <button type="submit" class="btn-submit" id="submitBtn">
            Enviar mensaje →
          </button>
        </div>

        <div class="form-status" id="formStatus" role="alert" aria-live="polite"></div>

      </form>
    </div><!-- /form-card -->

  </div><!-- /contact-inner -->
</section>

</main>
</BaseLayout>

<script define:vars={{ publicKey, serviceId, templateId, contactEmail }}>
  // ── Form submission via EmailJS
  (function () {
    emailjs.init(publicKey);

    const form      = document.getElementById('contactForm');
    const btn       = document.getElementById('submitBtn');
    const status    = document.getElementById('formStatus');

    // ── Inline validation helpers
    function setError(inputId, errId, show) {
      const input = document.getElementById(inputId);
      const err   = document.getElementById(errId);
      if (!input || !err) return;
      if (show) {
        input.setAttribute('aria-invalid', 'true');
        err.classList.add('visible');
      } else {
        input.removeAttribute('aria-invalid');
        err.classList.remove('visible');
      }
    }

    function validateForm(data) {
      let valid = true;
      if (!data.get('nombre').trim())                       { setError('nombre',  'err-nombre',  true);  valid = false; } else { setError('nombre',  'err-nombre',  false); }
      if (!data.get('empresa').trim())                      { setError('empresa', 'err-empresa', true);  valid = false; } else { setError('empresa', 'err-empresa', false); }
      const email = data.get('email').trim();
      if (!email || !/^[^\s@]+@[^\s@]+\.[^\s@]+$/.test(email)) { setError('email', 'err-email', true); valid = false; } else { setError('email', 'err-email', false); }
      if (!data.get('tema'))                                { setError('tema',    'err-tema',    true);  valid = false; } else { setError('tema',    'err-tema',    false); }
      if (!data.get('mensaje').trim())                      { setError('mensaje', 'err-mensaje', true);  valid = false; } else { setError('mensaje', 'err-mensaje', false); }
      return valid;
    }

    form.addEventListener('submit', async function (e) {
      e.preventDefault();

      const data = new FormData(form);
      if (!validateForm(data)) return;

      // Loading state
      btn.disabled = true;
      btn.textContent = 'Enviando…';
      status.className = 'form-status';
      status.textContent = '';

      const params = {
        from_name:    data.get('nombre'),
        from_company: data.get('empresa'),
        from_email:   data.get('email'),
        from_phone:   data.get('telefono') || '—',
        subject:      data.get('tema'),
        message:      data.get('mensaje'),
      };

      try {
        await emailjs.send(serviceId, templateId, params);
        status.className  = 'form-status success';
        status.textContent = '✓ Mensaje enviado. Un especialista te contactará en menos de 24 h.';
        form.reset();
        // Clear invalid states after reset
        form.querySelectorAll('[aria-invalid]').forEach(el => el.removeAttribute('aria-invalid'));
        form.querySelectorAll('.field-error.visible').forEach(el => el.classList.remove('visible'));
      } catch (err) {
        status.className  = 'form-status error';
        status.textContent = '✗ Hubo un problema al enviar. Escríbenos directamente a ' + contactEmail + ' o por WhatsApp.';
      } finally {
        btn.disabled    = false;
        btn.textContent = 'Enviar mensaje →';
        status.scrollIntoView({ behavior: 'smooth', block: 'nearest' });
      }
    });

    // ── Live validation on blur (better UX)
    ['nombre','empresa','email','tema','mensaje'].forEach(id => {
      const el = document.getElementById(id);
      if (!el) return;
      el.addEventListener('blur', () => {
        const val = el.value;
        const errId = 'err-' + id;
        if (id === 'email') {
          setError(id, errId, !val.trim() || !/^[^\s@]+@[^\s@]+\.[^\s@]+$/.test(val));
        } else {
          setError(id, errId, !val || !val.trim());
        }
      });
    });
  })();
</script>
